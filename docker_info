도커 버전 확인
> docker --version

도커 이미지 확인
> docker images

도커 docker-compose 파일 실행
> docker-compose up -d

Docker 컨테이너 목록 출력
> docker ps -a

도커 컨테이너 쉘 접속
> docker exec -it parksy-mysql bash
> docker exec -it parksy-mysql /bin/sh